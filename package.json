{
  "name": "breadcrumb",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Cross-machine capture and rehydration of opencode sessions (BRC-SPEC-002).",
  "scripts": {
    "test": "node --test test/*.test.ts",
    "typecheck": "tsc --noEmit",
    "crumb": "node probe/crumb.ts",
    "install:plugin": "node scripts/install.mjs"
  },
  "engines": {
    "node": ">=23.6"
  },
  "devDependencies": {
    "@opencode-ai/plugin": "1.17.13",
    "@types/node": "^24.0.0",
    "typescript": "^5.6.0"
  }
}
